import { rechunk, rechunk_ } from "./rechunk.js";
/**
 * Re-chunks the elements of the stream into chunks of
 * `n` elements each.
 * The last chunk might contain less than `n` elements
 *
 * @deprecated
 */
export declare const chunkN_: typeof rechunk_;
/**
 * Re-chunks the elements of the stream into chunks of
 * `n` elements each.
 * The last chunk might contain less than `n` elements
 *
 * @deprecated
 */
export declare const chunkN: typeof rechunk;
//# sourceMappingURL=chunkN.d.ts.map